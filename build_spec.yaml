version: 0.1

component: build

timeoutInSeconds: 600

shell: bash

steps:
  - type: Command
    name: "Build"
    command: |
      echo "Building project..."
      mvn clean install -DskipTests=true

  - type: Command
    name: "Run Regression Tests"
    command: |
      echo "Running tests..."
      mvn test
